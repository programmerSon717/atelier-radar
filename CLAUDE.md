# atelier-radar 작업 규칙

## 시작할 때
1. `REQUIREMENTS.md` 를 먼저 읽는다. 사용자가 이미 말한 요구가 전부 거기 있다.
2. `PENDING.md` 로 진행 상황을 확인한다.

## 새 요청이 오면
**작업하기 전에 `REQUIREMENTS.md` 에 먼저 적는다.** 적고 나서 작업한다.
끝나면 상태를 갱신한다. 못 한 건 못 했다고 적는다 — "아직 못 한 것" 절이 그래서 있다.

## 문구를 고칠 때
화면·메시지 문구의 원본은 `config/locales/{ko,en,zh_TW}.yaml` 이다.
**`site/ui.js` 는 생성물이다 — 직접 고치지 마라.** 고쳤으면 `tools/build_ui.py` 를 돌린다.
웹과 텔레그램은 같은 파일을 읽는다. 한쪽만 고치면 두 화면이 갈린다.

## 절대 규칙
- 공고에 없는 사실을 지어내지 않는다. 모르면 null, 화면에는 "확인 필요".
- 후보자는 **미국 대학 졸업 · 경력 0년 · 일본어 불가 · 한국어 초급**이다.
  "국내 정규 4년제 대학 졸업자" 같은 조건은 이 사람을 통째로 배제한다. 반드시 잡아낸다.
- 경력을 요구하는 공고는 라벨이 아니라 아예 제외한다 (인턴 제외).
- 공개 저장소다. 이름·연락처·학교명을 커밋하지 않는다.
  (텔레그램은 비공개 그룹이라 실명이 그대로 나간다. 가리는 건 `docs/` 로 나갈 때뿐이다)
- 텔레그램에 나가는 것은 **사이트에 실리는 것과 같아야 한다.**
  거르는 기준은 `tools/export_data.selected()` 하나뿐이다. 새 필터를 다른 데 만들지 마라.
